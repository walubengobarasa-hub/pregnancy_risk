Options -Indexes
RewriteEngine On

# If request already starts with /public, don't rewrite again (prevents loop)
RewriteRule ^public/ - [L]

# Send the base folder to /public/
RewriteRule ^$ public/ [L]

# Forward everything else to /public/...
RewriteRule ^(.+)$ public/$1 [L,QSA]
